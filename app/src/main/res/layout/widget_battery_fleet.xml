<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_fleet_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- Top Header Bar -->
    <LinearLayout
        android:id="@+id/widget_fleet_header_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingBottom="6dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="BATTERY FLEET MONITOR"
                android:textColor="#EEF8FF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:letterSpacing="0.04" />

            <TextView
                android:id="@+id/widget_fleet_subtitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Multi-Brand • Live Telemetry"
                android:textColor="#00DCFF"
                android:textSize="10sp"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_fleet_count_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="● 3 Connected"
            android:textColor="#00F5AA"
            android:textSize="11sp"
            android:textStyle="bold"
            android:background="@drawable/widget_pill_in_bg"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="3dp"
            android:paddingBottom="3dp" />
    </LinearLayout>

    <!-- Scrollable Multi-Battery List -->
    <ListView
        android:id="@+id/widget_fleet_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:scrollbars="vertical" />

    <!-- Empty State View -->
    <TextView
        android:id="@+id/widget_fleet_empty"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="No batteries detected yet.\nOpen Battery Manager to scan."
        android:textColor="#8EB0C2"
        android:textSize="12sp"
        android:visibility="gone"
        android:padding="16dp" />
</LinearLayout>
